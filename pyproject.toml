[project]
name = "python-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "setuptools>=70.3.0",
]

[tool.coverage.run]
omit = [
    "tests/*",
    "*/tests/*",
    "*tests/*"
]

[tool.pytest.ini_options]
# Run coverage report for every `uv run pytest`
# This project will remain small, so this is ok.
addopts = "--cov-report=term-missing"
testpaths = ["tests"]

[tool.ruff]
# The directories Ruff should analyze
# This prevents it from accidentally linting virtual environments or build folders
src = ["logfetcher", "tests"]
line-length = 88

[tool.mypy]
# The directories Mypy should type-check
files = ["logfetcher", "tests"]
# Enforce strict type checking across the project
strict = true

# Be slightly more lenient with type checking inside the test suite
[[tool.mypy.overrides]]
module = "tests.*"
strict = false

[tool.setuptools]
packages = ["logfetcher"]

[dependency-groups]
dev = [
    "pyfakefs>=6.1.1",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pylint>=3.3.1",
    "genbadge[coverage,tests]>=1.1.1",
]
